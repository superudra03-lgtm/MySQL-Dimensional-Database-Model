This project is based on the work for the star relational schema I created. 

1) Build the data sources defined in MySQL. Making sure to store the two data sources in two separate databases. These two databases will serve as an operational database. 

2) Using the dimensional model, I built a data warehouse in MySQL in a separate database. I made sure to show the SQL code for building both the dimensional and fact tables. Additionally, I showed the SQL code to insert data from the two aforementioned data sources into the data warehouse.

So, in total, I created three databases: 1) Big Z Inc. Order Database (this is an operational database, which is designed to keep track of orders, 2) Human Resources Database this is an operational database, which contains a single table that keeps track of employee data, and 3) Big Z Data Warehouse this is the analytical database, which will receive data from the two data sources detailed above. 
